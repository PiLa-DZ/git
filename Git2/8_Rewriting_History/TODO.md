# Rewriting History

- Rewriting History (Fixing mistakes, Removing sensitive data, Simplifying complex histories)
  - `git commit --amend`
  - `git rebase`
  - Squash

  - `git filter-branch` `git filter-repo` (Filter types, Preserving original data, Rewriting specific branches)
  - `git push --force`
