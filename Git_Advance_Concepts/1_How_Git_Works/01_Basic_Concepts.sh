# Database (.git)
# .gitignore
# Initialization (git init)
# Local Machine <-{SSH   }-> Remote Server
# Local Repo    <-{Remote}-> Remote Repo
# Config (--global, --local, --system)
# Workflow {Workflow Dir, Staging Area, Repo}
# Log {Hash, Author, Date, Message}
# Branching {List, Create, Switch, Rename, Delete(--soft,--hard), Merge}
